# 智慧校园综测服务与活动管理系统运行部署说明

本文档面向第一次拿到代码的同学，目标是让项目在一台新电脑上顺利运行起来。系统分为后端、前端和 MySQL 数据库三部分：

- 后端：Spring Boot，默认端口 `8080`
- 前端：Vue3 + Element Plus，默认端口 `5173`
- 数据库：MySQL 8.0，数据库名 `smart_campus`

AI 问答模块已接入 OpenAI 兼容接口，默认关闭。普通功能不依赖 AI 配置；如需启用问答，需要额外配置 `AI_ENABLED=true` 和 `AI_API_KEY`。

## 1. 项目目录说明

假设项目放在：

```text
D:\smart_campus_system
```

主要目录如下：

```text
D:\smart_campus_system
├─ backend                 后端 Spring Boot 项目
├─ front                   前端 Vue3 项目
├─ scripts
│  └─ init_mysql.sql       MySQL 建库建表和初始化数据脚本
├─ README.md               简要说明
└─ 运行部署说明.md          当前详细运行文档
```

## 2. 必需软件

建议使用以下版本或兼容版本：

| 软件 | 建议版本 | 用途 |
| --- | --- | --- |
| JDK | 17 | 运行 Spring Boot 后端 |
| Maven | 3.8+ / 3.9+ | 下载后端依赖、启动后端 |
| MySQL | 8.0+ | 存储系统数据 |
| Node.js | 18+ / 20+ / 22+ | 运行前端 |
| npm | 随 Node.js 安装 | 安装前端依赖 |
| IntelliJ IDEA | 2023+ / 2024+ | 推荐运行后端 |
| VS Code | 最新版 | 推荐运行前端 |

本机已验证过的软件路径如下，仅供参考。其他电脑可以安装到不同目录，只要命令能正常执行即可。

```text
Node.js: D:\web\nodejs\node.exe
npm:     D:\web\nodejs\npm.cmd
JDK:     D:\Idea\jdk-17_windows-x64_bin\jdk-17.0.9
Maven:   D:\Idea\apache-maven-3.9.9-bin\apache-maven-3.9.9
MySQL:   D:\mysql\mysql-8.0.42-winx64\bin\mysql.exe
```

## 3. 数据库初始化

### 3.1 确认 MySQL 服务正在运行

Windows 可以在“服务”里确认 MySQL 服务已经启动，也可以在 PowerShell 中执行：

```powershell
Get-Service MySQL
```

如果状态不是 `Running`，可以启动：

```powershell
Start-Service MySQL
```

### 3.2 导入初始化脚本

打开 PowerShell，进入项目根目录：

```powershell
cd D:\smart_campus_system
```

执行初始化脚本。把 `你的MySQL密码` 换成自己电脑的 MySQL root 密码：

```powershell
& 'D:\mysql\mysql-8.0.42-winx64\bin\mysql.exe' --default-character-set=utf8mb4 -uroot -p你的MySQL密码 --execute="source D:/smart_campus_system/scripts/init_mysql.sql"
```

注意：

- `--default-character-set=utf8mb4` 必须带上，否则 Windows 下导入中文数据可能报 `Incorrect string value`。
- `source` 后面的路径建议使用 `/`，例如 `D:/smart_campus_system/scripts/init_mysql.sql`。
- 该脚本会重建 `smart_campus` 数据库，并写入初始演示数据。重复执行会清空旧数据并恢复初始状态。

### 3.3 验证数据库是否导入成功

```powershell
& 'D:\mysql\mysql-8.0.42-winx64\bin\mysql.exe' --default-character-set=utf8mb4 -uroot -p你的MySQL密码 --execute="use smart_campus; select count(*) users from user_account; select count(*) activities from activity;"
```

正常会看到：

```text
users
5

activities
5
```

## 4. 后端运行方式一：使用 IntelliJ IDEA

这是推荐方式。

### 4.1 打开后端项目

1. 打开 IntelliJ IDEA。
2. 选择 `Open`。
3. 打开目录：

```text
D:\smart_campus_system\backend
```

4. 等待 IDEA 自动识别 Maven 项目并下载依赖。

如果 IDEA 提示选择 JDK，请选择 JDK 17。

### 4.2 配置 Maven

如果 IDEA 没有自动识别 Maven，可以检查：

```text
File -> Settings -> Build, Execution, Deployment -> Build Tools -> Maven
```

可以设置：

```text
Maven home path: D:\Idea\apache-maven-3.9.9-bin\apache-maven-3.9.9
```

其他电脑按自己的 Maven 安装路径填写即可。

### 4.3 配置数据库密码环境变量

后端配置文件位于：

```text
D:\smart_campus_system\backend\src\main\resources\application.yml
```

其中数据库密码读取环境变量：

```yaml
password: ${DB_PASSWORD:}
```

也就是说，运行后端时需要配置：

```text
DB_PASSWORD=你的MySQL密码
```

在 IDEA 中配置方法：

1. 打开启动类：

```text
D:\smart_campus_system\backend\src\main\java\com\example\campus\SmartCampusApplication.java
```

2. 点击右上角运行配置下拉框。
3. 选择 `Edit Configurations...`。
4. 选中 `SmartCampusApplication`。
5. 找到 `Environment variables`。
6. 填入：

```text
DB_PASSWORD=你的MySQL密码
```

例如你的 MySQL 密码是 `123456`，就填：

```text
DB_PASSWORD=123456
```

7. 点击 `Apply`，再点击 `OK`。

### 4.4 启动后端

在 `SmartCampusApplication.java` 中点击绿色运行按钮。

启动成功后，控制台会看到类似信息：

```text
Tomcat started on port 8080
Started SmartCampusApplication
```

浏览器访问：

```text
http://localhost:8080/api/health
```

正常返回：

```json
{"code":0,"message":"ok","data":{"status":"UP"}}
```

## 5. 后端运行方式二：使用命令行

如果不用 IDEA，也可以在 PowerShell 中运行。

进入后端目录：

```powershell
cd D:\smart_campus_system\backend
```

设置数据库密码：

```powershell
$env:DB_PASSWORD='你的MySQL密码'
```

启动后端：

```powershell
& 'D:\Idea\apache-maven-3.9.9-bin\apache-maven-3.9.9\bin\mvn.cmd' spring-boot:run
```

如果 Maven 已经配置到系统环境变量，也可以直接执行：

```powershell
mvn spring-boot:run
```

## 6. 前端运行方式：使用 VS Code

### 6.1 打开前端项目

1. 打开 VS Code。
2. 选择 `File -> Open Folder...`。
3. 打开目录：

```text
D:\smart_campus_system\front
```

### 6.2 安装前端依赖

打开 VS Code 终端，执行：

```powershell
& 'D:\web\nodejs\npm.cmd' install
```

如果电脑已经把 npm 配到环境变量，也可以执行：

```powershell
npm install
```

注意：PowerShell 默认可能禁止执行 `npm.ps1`，所以推荐使用 `npm.cmd`。

### 6.3 启动前端

```powershell
& 'D:\web\nodejs\npm.cmd' run dev
```

如果 npm 已经配置到环境变量：

```powershell
npm run dev
```

启动成功后会看到类似：

```text
Local: http://localhost:5173/
```

浏览器访问：

```text
http://localhost:5173
```

前端已配置代理，访问 `/api` 时会自动转发到：

```text
http://localhost:8080
```

所以运行前端时，请确保后端也已经启动。

## 7. 默认测试账号

初始化脚本内置了以下账号，密码均为：

```text
123456
```

| 账号 | 角色 | 说明 |
| --- | --- | --- |
| admin | 管理员 | 学生管理、负责人审批、组织审批、积分审核 |
| leader1 | 组织负责人 | 管理软件创新协会、发布活动、审批成员、录分 |
| leader2 | 组织负责人 | 管理青年志愿者服务队 |
| student1 | 普通学生 | 浏览活动、报名、申请加入组织、查看积分 |
| student2 | 普通学生 | 普通学生演示账号 |

## 8. 页面功能说明

### 8.1 登录 / 注册

访问：

```text
http://localhost:5173/login
```

功能：

- 用户登录
- 用户注册
- 登录后根据角色显示不同菜单
- 未登录用户不能访问系统内部页面

### 8.2 学生首页

访问：

```text
http://localhost:5173/home
```

功能：

- 展示近期活动
- 展示有效报名数量
- 展示待审核积分数量
- 展示审核通过积分总分
- 提交成为组织负责人申请
- 查看负责人申请进度
- 如果申请通过，显示组织负责人管理入口

### 8.3 活动中心

访问：

```text
http://localhost:5173/activities
```

功能：

- 活动列表
- 按关键词筛选
- 按活动类型筛选
- 按活动状态筛选
- 分页展示
- 显示活动名称、类型、主办组织、时间、地点、名额、剩余名额、基础分、状态
- 点击活动进入详情页

### 8.4 活动详情

访问示例：

```text
http://localhost:5173/activities/1
```

功能：

- 查看活动简介
- 查看活动要求
- 查看报名截止时间
- 查看当前报名状态
- 查看基础分值和身份权重
- 报名活动
- 取消报名

报名时后端会校验：

- 活动是否存在
- 活动是否处于 `OPEN`
- 是否超过报名截止时间
- 名额是否已满
- 当前学生是否已经报名

### 8.5 组织中心

访问：

```text
http://localhost:5173/organizations
```

功能：

- 查看组织列表
- 查看组织类型、负责人、状态、创建时间
- 查看当前用户与组织的关系
- 点击组织进入组织详情

### 8.6 组织详情

访问示例：

```text
http://localhost:5173/organizations/1
```

功能：

- 查看组织简介
- 查看组织负责人
- 查看组织联系方式
- 查看该组织发布的活动
- 普通学生申请加入组织
- 组织负责人进入组织管理入口

加入组织申请由对应组织负责人审批，不由管理员审批。

### 8.7 我的活动与积分

访问：

```text
http://localhost:5173/mine
```

功能：

- 查看报名记录
- 查看报名状态
- 查看签到/参与状态
- 查看个人积分总分
- 查看积分明细
- 查看基础分、身份权重、最终得分、审核状态、驳回原因

总分只统计审核通过的积分记录，待审核记录不计入总分。

### 8.8 AI 助手

访问：

```text
http://localhost:5173/ai
```

功能：

- 读取当前登录用户的活动、报名、组织和积分上下文
- 调用 OpenAI 兼容的 `/chat/completions` 接口生成回答
- 保存用户问题、模型回答、模型名称、token 用量和耗时
- 查看当前用户最近 20 条问答历史
- 未启用 AI 或未配置密钥时，接口返回明确提示，不影响其他业务模块

默认情况下 AI 关闭，普通运行无需配置。需要启用时，在启动后端前设置：

Windows PowerShell：

```powershell
$env:AI_ENABLED="true"
$env:AI_API_KEY="你的模型服务密钥"
$env:AI_API_BASE_URL="https://api.openai.com/v1"
$env:AI_MODEL="gpt-4o-mini"
```

Linux/macOS：

```bash
export AI_ENABLED=true
export AI_API_KEY=你的模型服务密钥
export AI_API_BASE_URL=https://api.openai.com/v1
export AI_MODEL=gpt-4o-mini
```

如果使用其他兼容 OpenAI 格式的模型服务，只需要把 `AI_API_BASE_URL` 改为对应服务的基础地址。

### 8.9 组织负责人管理

访问：

```text
http://localhost:5173/leader
```

需要 `ORG_LEADER` 或 `ADMIN` 角色。

功能：

- 查看组织成立申请进度
- 查看驳回原因
- 申请成立组织
- 维护自己负责的组织信息
- 查看组织成员列表
- 审批成员加入申请
- 驳回成员申请并填写原因
- 发布活动
- 编辑活动
- 维护活动状态：发布、截止、结束、下架
- 查看活动报名名单
- 设置签到状态
- 对已结束活动中的报名学生录分

录分时后端会自动判断身份：

- 非组织成员：普通学生权重
- 组织成员：组织成员权重
- 组织负责人：负责人权重

### 8.10 管理员学生管理

访问：

```text
http://localhost:5173/admin/students
```

需要 `ADMIN` 角色。

功能：

- 查看学生列表
- 按学号、姓名、账号查询
- 按账号状态筛选
- 按负责人申请状态筛选
- 修改学生基础信息
- 启用或禁用账号
- 审批学生成为组织负责人申请
- 驳回申请并填写原因
- 查看学生详情，包括报名活动、加入组织、负责组织、积分记录

禁用账号后，用户不能登录，但历史报名、组织申请和积分记录不会删除。

### 8.11 管理员组织与审核

访问：

```text
http://localhost:5173/admin/organizations
```

需要 `ADMIN` 角色。

功能：

- 查看组织成立申请列表
- 审核组织名称、成立理由、申请人信息
- 审批通过后创建组织并设置申请人为负责人
- 驳回组织申请并填写原因
- 查看组织列表
- 启用或停用组织
- 查看组织详情

停用组织后不能继续发布新活动，但历史活动、成员记录和积分记录保留。

### 8.12 管理员积分审核

访问：

```text
http://localhost:5173/admin/scores
```

需要 `ADMIN` 角色。

功能：

- 查看积分记录
- 按审核状态筛选
- 审核通过积分
- 驳回积分并填写原因
- 查看和维护积分规则
- 设置普通学生、组织成员、组织负责人的身份权重

### 8.13 学生事务申请

访问：

```text
http://localhost:5173/affairs
```

功能：

- 普通学生可以申请桌椅、海报张贴位、活动物资等事务
- 组织负责人可以额外为自己负责的启用组织申请教室、报告厅等资源
- 申请人可以查看自己的申请进度、审批说明和驳回原因
- 待审核申请可以由申请人主动撤销
- 管理员可以审批全部事务申请
- 管理员审批教室、场地等指定资源时，后端会校验时间段是否冲突
- 申请提交、审批通过、审批驳回都会写入消息通知中心

### 8.14 消息通知中心

访问：

```text
http://localhost:5173/messages
```

功能：

- 查看个人站内消息
- 查看未读消息数量
- 将单条消息标为已读
- 将全部消息标为已读
- 接收负责人申请、组织申请、成员审核、积分审核、事务审批、评价回复等业务消息
- 管理员可以发布公告
- 公告可以面向全部用户、普通学生、组织负责人或管理员

### 8.15 活动评价反馈

访问：

```text
http://localhost:5173/feedbacks
```

功能：

- 已有效报名、已签到、且活动已结束的用户可以提交评价
- 每个用户对同一活动只能评价一次
- 评价支持 1 到 5 星评分、文字反馈和匿名展示
- 用户可以查看自己的评价和组织回复
- 活动详情页会展示公开可见评价
- 组织负责人可以管理自己组织活动的评价并回复
- 管理员可以管理全部活动评价
- 负责人或管理员可以隐藏不适合公开展示的评价

## 9. 常用开发命令

### 9.1 后端编译检查

```powershell
cd D:\smart_campus_system\backend
& 'D:\Idea\apache-maven-3.9.9-bin\apache-maven-3.9.9\bin\mvn.cmd' -DskipTests package
```

### 9.2 前端构建检查

```powershell
cd D:\smart_campus_system\front
& 'D:\web\nodejs\npm.cmd' run build
```

### 9.3 重新初始化数据库

```powershell
& 'D:\mysql\mysql-8.0.42-winx64\bin\mysql.exe' --default-character-set=utf8mb4 -uroot -p你的MySQL密码 --execute="source D:/smart_campus_system/scripts/init_mysql.sql"
```

## 10. 常见问题处理

### 10.1 端口 8080 被占用

说明后端端口被其他进程占用了。

查看占用：

```powershell
Get-NetTCPConnection -LocalPort 8080
```

结束占用进程：

```powershell
$p = Get-NetTCPConnection -LocalPort 8080
Stop-Process -Id $p.OwningProcess -Force
```

### 10.2 端口 5173 被占用

查看占用：

```powershell
Get-NetTCPConnection -LocalPort 5173
```

结束占用进程：

```powershell
$p = Get-NetTCPConnection -LocalPort 5173
Stop-Process -Id $p.OwningProcess -Force
```

也可以换端口启动前端：

```powershell
& 'D:\web\nodejs\npm.cmd' run dev -- --port 5174
```

### 10.3 后端启动时报数据库连接失败

常见原因：

- MySQL 服务没有启动
- MySQL 密码不对
- IDEA 没有配置 `DB_PASSWORD`
- 数据库还没有初始化

解决方法：

1. 确认 MySQL 服务运行。
2. 用 mysql 命令确认密码正确。
3. 在 IDEA 运行配置里添加：

```text
DB_PASSWORD=你的MySQL密码
```

4. 重新导入 `scripts/init_mysql.sql`。

### 10.4 导入 SQL 出现 Incorrect string value

这是中文字符集问题。导入时必须加：

```text
--default-character-set=utf8mb4
```

正确示例：

```powershell
& 'D:\mysql\mysql-8.0.42-winx64\bin\mysql.exe' --default-character-set=utf8mb4 -uroot -p你的MySQL密码 --execute="source D:/smart_campus_system/scripts/init_mysql.sql"
```

### 10.5 PowerShell 不能执行 npm

不要用 `npm.ps1`，使用 `npm.cmd`：

```powershell
& 'D:\web\nodejs\npm.cmd' install
& 'D:\web\nodejs\npm.cmd' run dev
```

### 10.6 前端页面能打开，但接口报错

检查后端是否启动：

```text
http://localhost:8080/api/health
```

如果打不开，先启动后端。

如果后端正常，再检查前端项目的代理配置：

```text
D:\smart_campus_system\front\vite.config.js
```

里面应有：

```js
proxy: {
  '/api': {
    target: 'http://localhost:8080',
    changeOrigin: true
  }
}
```

### 10.7 点击报名提示时间类型错误

当前代码已经修复该问题。修复点是：

```text
D:\smart_campus_system\backend\src\main\java\com\example\campus\Db.java
D:\smart_campus_system\backend\src\main\java\com\example\campus\ActivityController.java
```

如果其他电脑仍然出现类似问题，请确认拿到的是最新代码，并重新启动后端。

## 11. 推荐启动顺序

每次运行项目时，按以下顺序：

1. 启动 MySQL 服务。
2. 必要时导入 `scripts/init_mysql.sql`。
3. 在 IDEA 中启动后端。
4. 在浏览器访问 `http://localhost:8080/api/health` 验证后端。
5. 在 VS Code 中启动前端。
6. 在浏览器访问 `http://localhost:5173`。
7. 使用测试账号登录。

## 12. 交付检查清单

给其他同学或老师演示前，可以检查：

- MySQL 服务是否运行
- `smart_campus` 数据库是否存在
- 后端 `8080` 是否启动
- 前端 `5173` 是否启动
- `admin / 123456` 是否能登录
- `student1 / 123456` 是否能报名活动
- `leader1 / 123456` 是否能进入负责人管理
- 管理员是否能审批负责人申请、组织申请和积分记录
- 学生是否能提交事务申请，管理员是否能审批事务申请
- 消息通知中心是否能查看未读消息并标记已读
- 已签到且已结束活动是否能提交评价，负责人是否能回复评价
