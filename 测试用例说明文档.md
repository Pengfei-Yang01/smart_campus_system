# 智慧校园系统 — 完整性约束测试说明

## 概述

本测试套件针对智慧校园综测服务与活动管理系统的核心业务约束进行**集成测试**，验证系统的数据完整性、业务规则正确性和权限控制有效性。

测试层采用 **Spring Boot `@SpringBootTest` + `TestRestTemplate`**，连接独立 MySQL 测试数据库 `smart_campus_test`，真实执行 SQL 约束和业务逻辑。

## 技术栈

| 项目 | 版本 |
|------|------|
| Java | 17 |
| Spring Boot | 3.3.6 |
| JUnit | 5 (Jupiter) |
| AssertJ | 3.x |
| TestRestTemplate | Spring Boot Test |
| MySQL | 8.0+ / 9.x |
| Apache HttpClient5 | 5.x (PATCH 方法支持) |

## 目录结构

```
src/test/
├── java/com/example/campus/
│   ├── SmartCampusApplicationTests.java       # 上下文加载测试
│   ├── auth/AuthControllerTest.java           # 认证模块测试
│   ├── activity/ActivityControllerTest.java   # 活动模块测试
│   ├── organization/OrganizationControllerTest.java  # 组织模块测试
│   ├── admin/AdminControllerTest.java         # 管理员模块测试
│   ├── score/ScoreControllerTest.java         # 积分模块测试
│   └── config/TestRestConfig.java             # PATCH 支持配置
└── resources/
    ├── application-test.yml                   # 测试数据库配置
    └── test-data/reset.sql                    # Schema + 种子数据
```

## 运行方式

### 前置条件

- MySQL 服务运行中
- 配置好 `DB_PASSWORD` 环境变量

### 运行全部测试

```bash
cd backend
DB_PASSWORD=你的MySQL密码 mvn test -Dspring.profiles.active=test
```

### 运行单个测试类

```bash
DB_PASSWORD=你的MySQL密码 mvn test -Dspring.profiles.active=test -Dtest=ActivityControllerTest
```

测试框架会自动创建 `smart_campus_test` 数据库（如果不存在），每个测试类运行前通过 `reset.sql` 完全重建表结构和种子数据，确保环境隔离。

---

## 测试用例清单

总计 **24 个测试用例**，分布在 6 个测试类中。

---

### 1. SmartCampusApplicationTests（1 个用例）

**验证目的**：确保 Spring 应用上下文能正常加载，所有 Bean、数据库连接、配置均正确。

| 编号 | 测试方法 | 预期结果 |
|------|---------|---------|
| 1 | `contextLoads` | 应用启动不抛出异常 |

---

### 2. AuthControllerTest（5 个用例）

**验证目的**：认证模块的登录、注册、令牌校验和账号状态约束。

| 编号 | 测试方法 | 场景 | 预期结果 |
|------|---------|------|---------|
| 2 | `login_成功` | 使用正确凭证登录 | 200，返回 `token` 和 `user` 信息 |
| 3 | `login_密码错误` | 使用错误密码登录 | 400，消息包含"账号或密码错误" |
| 4 | `login_账号已禁用` | 被管理员禁用的账号登录 | 400，消息包含"账号已被禁用" |
| 5 | `register_成功注册` | 注册新学生账号 | 200，返回 `token` 和 `user` |
| 6 | `me_获取当前用户` | 携带有效令牌访问 `/api/auth/me` | 200，返回当前用户信息 |

**约束覆盖**：密码校验、账号状态校验、令牌认证

---

### 3. ActivityControllerTest（8 个用例）

**验证目的**：活动 CRUD、状态机流转、报名约束（角色、容量、截止时间）。

| 编号 | 测试方法 | 场景 | 预期结果 |
|------|---------|------|---------|
| 7 | `create_负责人成功创建` | 组织负责人创建活动 | 200，返回 `activityId` |
| 8 | `create_学生无权限` | 普通学生创建活动 | 400，提示"只有管理员或组织负责人可以发布活动" |
| 9 | `status_有效状态链` | DRAFT→OPEN→CLOSED→FINISHED→OFFLINE | 每步均返回 200，`status` 与目标一致 |
| 10 | `status_无效跳转` | DRAFT→FINISHED（跳过中间状态） | 400，提示"不允许" |
| 11 | `register_正常报名` | 报名开放中的活动 | 200 |
| 12 | `register_活动已满` | 容量已满时报名 | 400，提示"名额已满" |
| 13 | `register_报名截止已过` | 截止时间后报名 | 400，提示"报名时间已截止" |
| 14 | `register_取消报名` | 取消有效报名 | 200 |

**约束覆盖**：
- **角色权限**：只有管理员/组织负责人可创建活动
- **状态机**：DRAFT→OPEN→CLOSED→FINISHED→OFFLINE 单向流转，禁止跳转
- **容量约束**：`registered_count <= capacity`，超限拒绝
- **时间约束**：`registration_deadline <= start_time`，过期拒绝
- **数据库约束**：`check (capacity > 0)`、`check (end_time >= start_time)`

#### 活动状态流转图

```
    ┌─────────┐
    │  DRAFT  │──────→ OFFLINE
    └────┬────┘
         │
         ▼
    ┌─────────┐
    │  OPEN   │──────→ OFFLINE
    └────┬────┘
         │
         ▼
    ┌─────────┐
    │ CLOSED  │──────→ OFFLINE
    └────┬────┘
         │
         ▼
    ┌──────────┐
    │ FINISHED │──────→ OFFLINE
    └──────────┘
```

---

### 4. OrganizationControllerTest（4 个用例）

**验证目的**：组织申请、加入和成员审核的业务约束。

| 编号 | 测试方法 | 场景 | 预期结果 |
|------|---------|------|---------|
| 15 | `apply_负责人申请创建组织` | 组织负责人提交组织创建申请 | 200 |
| 16 | `apply_学生无权限` | 普通学生提交组织创建申请 | 400，提示"只有组织负责人可以申请创建组织" |
| 17 | `join_加入活跃组织` | 学生申请加入活跃组织 | 200，`join_status` 为 PENDING |
| 18 | `auditMember_通过审核` | 负责人通过成员申请 | 200，`join_status` 变为 APPROVED |

**约束覆盖**：
- **角色权限**：只有组织负责人可申请创建组织
- **成员管理**：加入申请状态机 PENDING → APPROVED/REJECTED
- **组织状态**：只有 ACTIVE 的组织允许加入

#### 成员状态流转

```
    ┌─────────┐
    │ PENDING │──────→ APPROVED
    └────┬────┘
         │
         └──────→ REJECTED → (可重新申请 → PENDING)
```

---

### 5. AdminControllerTest（4 个用例）

**验证目的**：管理员权限、数据校验、级联操作和角色授予。

| 编号 | 测试方法 | 场景 | 预期结果 |
|------|---------|------|---------|
| 19 | `stats_管理员统计` | 管理员查看系统统计 | 200，返回完整计数（6 用户/5 活动/3 组织） |
| 20 | `updateStudent_手机号格式校验` | 修改学生信息时填非法手机号 | 400，提示"电话号码必须为11位数字" |
| 21 | `disableOrg_级联停用活动` | 停用组织 | 200，组织下活动变为 `OFFLINE` |
| 22 | `approveLeaderApply_角色授予` | 通过负责人申请 | 200，该用户获得 `ORG_LEADER` 角色 |

**约束覆盖**：
- **输入校验**：手机号必须为 11 位数字（`^\d{11}$`）
- **级联操作**：组织停用 → 组织下所有活动置为 `OFFLINE` → 取消有效报名
- **角色授予**：审核通过负责人申请 → 自动插入 `user_role` 记录
- **角色权限**：仅 ADMIN 可执行管理操作

---

### 6. ScoreControllerTest（2 个用例）

**验证目的**：积分录入的业务约束。

| 编号 | 测试方法 | 场景 | 预期结果 |
|------|---------|------|---------|
| 23 | `record_已结束活动录分成功` | 为已结束活动中有报名的学生录分 | 200，返回 `finalScore` 和 `scoreId` |
| 24 | `record_未结束活动录分失败` | 为未结束活动录分 | 400，提示"活动结束后才能录入积分" |

**约束覆盖**：
- **活动状态**：仅 `FINISHED` 状态的活动可录分
- **报名校验**：只有报名且未取消的学生才能获得积分
- **身份权重**：`finalScore = baseScore × weight`（普通/成员/负责人）

#### 积分计算规则

| 身份类型 | 权重 | 说明 |
|---------|------|------|
| `NORMAL` | `normal_weight` (默认 1.00) | 普通参与者 |
| `ORG_MEMBER` | `member_weight` (默认 1.20) | 组织成员 |
| `ORG_LEADER` | `leader_weight` (默认 1.50) | 组织负责人 |

积分记录提交后进入 `PENDING` 状态，等待管理员审核 → `APPROVED` / `REJECTED`。

---

## 种子数据说明

`reset.sql` 构建了完整的测试数据集，包含 6 个用户、3 个组织、5 个活动和各类相关记录。

### 用户与角色

| 用户 ID | 用户名 | 角色 | 说明 |
|---------|--------|------|------|
| 1 | admin | STUDENT + ADMIN | 管理员 |
| 2 | student1 | STUDENT | 普通学生（有 PENDING 负责人申请） |
| 3 | student2 | STUDENT | 普通学生（有 PENDING 成员申请） |
| 4 | leader1 | STUDENT + ORG_LEADER | 组织1、3 负责人 |
| 5 | leader2 | STUDENT + ORG_LEADER | 组织2 负责人 |
| 6 | disabled_stu | STUDENT（DISABLED） | 账号已禁用 |

### 组织

| 组织 ID | 名称 | 状态 | 负责人 |
|---------|------|------|--------|
| 1 | 软件创新协会 | ACTIVE | leader1 |
| 2 | 青年志愿者服务队 | ACTIVE | leader2 |
| 3 | 测试停用组织 | DISABLED | leader1 |

### 活动

| 活动 ID | 名称 | 状态 | 组织 | 容量 | 已报名 | 截止时间 |
|---------|------|------|------|------|--------|---------|
| 1 | Spring Boot 实战工作坊 | OPEN | 1 (ACTIVE) | 60 | 2 | 2026-07-24 |
| 2 | 校园环保志愿服务 | FINISHED | 2 (ACTIVE) | 80 | 1 | 2026-01-14 |
| 3 | 算法训练营预热赛 | DRAFT | 1 (ACTIVE) | 40 | 0 | 2026-08-09 |
| 4 | 活动-禁用组织测试 | OPEN | 3 (DISABLED) | 50 | 0 | 2026-07-19 |
| 5 | 活动-已满员测试 | OPEN | 1 (ACTIVE) | 1 | 1 | 2025-12-31 |

---

## 常见问题

### Q: 测试报错 "Access denied for user 'root'@'localhost'"

MySQL 连接配置在 `application-test.yml` 中，默认用户名为 `root`，密码通过环境变量 `DB_PASSWORD` 传入。确认你的 MySQL 密码已正确设置：

```bash
export DB_PASSWORD=your_password   # Linux/macOS
set DB_PASSWORD=your_password      # Windows cmd
$env:DB_PASSWORD="your_password"   # Windows PowerShell
```

### Q: PATCH 请求失败

`TestRestTemplate` 的默认 HTTP 客户端不支持 PATCH 方法。解决方案：

1. 项目中已添加 `httpclient5` 依赖
2. 配置类 `TestRestConfig.java` 中设置了 `HttpComponentsClientHttpRequestFactory`

如果仍有问题，检查依赖是否下载成功：

```bash
cd backend
mvn dependency:resolve
```

### Q: 测试数据污染

每次运行测试类时，`@Sql(scripts = "/test-data/reset.sql", executionPhase = BEFORE_TEST_CLASS)` 会重建全部表结构和种子数据。不同测试类之间完全隔离，同一测试类内的测试方法共享初始数据。

### Q: 添加新的测试用例

1. 在 `reset.sql` 中添加或调整种子数据
2. 在对应的测试类中新增 `@Test` 方法
3. 按需使用 `@Sql(statements=..., executionPhase = BEFORE_TEST_METHOD)` 添加临时数据
