# 完整性约束测试说明

## 1. 测试目标

本测试用于验证校园活动管理系统的数据库完整性约束是否有效，包括：

- 实体完整性：主键、唯一键不允许重复。
- 参照完整性：外键必须引用真实存在的主表数据。
- 用户自定义完整性：容量、时间、分值、权重等业务字段必须满足检查约束。
- 枚举完整性：状态字段只能使用数据库定义的枚举值。
- 非空完整性：核心业务字段不能为空。

## 2. 测试环境与数据准备

测试类：`backend/src/test/java/com/example/campus/integrity/IntegrityConstraintTest.java`

测试数据库配置：`backend/src/test/resources/application-test.yml`

测试数据重置脚本：`backend/src/test/resources/test-data/reset.sql`

每个测试方法执行前都会重新执行 `reset.sql`，重建 `smart_campus_test` 数据库表并写入真实业务数据，包括：

- 6 个用户：管理员、普通学生、组织负责人、禁用学生。
- 3 个组织：2 个启用组织、1 个停用组织。
- 5 个活动：开放活动、结束活动、草稿活动、停用组织活动、满员/过期活动。
- 多条报名记录、组织成员记录、积分规则和积分记录。

这样每个测试用例都在干净、可重复的测试环境中运行。

## 3. 测试过程设计

完整性约束测试不依赖前端页面，也不只依赖后端业务判断，而是直接通过 `JdbcTemplate` 向测试数据库写入非法数据。

测试步骤为：

1. 执行 `reset.sql`，准备完整测试数据。
2. 执行一条违反数据库约束的 SQL。
3. 断言数据库抛出 `DataAccessException`。
4. 再次查询目标表行数或字段值。
5. 验证非法数据没有写入，原始数据没有被破坏。

## 4. 测试用例与实际测试数据

| 用例编号 | 约束类型 | 实际测试数据 | 预期结果 | 结果验证 |
| --- | --- | --- | --- | --- |
| IC-01 | 唯一约束 | 插入重复用户名 `student1` | 写入失败 | `user_account` 行数不变 |
| IC-02 | 唯一约束 | 插入重复学号 `S2023001` | 写入失败 | `user_account` 行数不变 |
| IC-03 | 唯一约束 | 插入已存在的组织名称 | 写入失败 | `organization` 行数不变 |
| IC-04 | 唯一约束 | 学生 `2` 重复报名活动 `1` | 写入失败 | `registration` 行数不变 |
| IC-05 | 唯一约束 | 活动类型 `1` 再插入一条启用积分规则 | 写入失败 | `score_rule` 行数不变 |
| IC-06 | 外键约束 | 为不存在用户 `9999` 插入学生档案 | 写入失败 | `student_profile` 行数不变 |
| IC-07 | 外键约束 | 为不存在负责人 `9999` 创建组织 | 写入失败 | `organization` 行数不变 |
| IC-08 | 外键约束 | 为不存在组织 `9999` 创建活动 | 写入失败 | `activity` 行数不变 |
| IC-09 | 外键约束 | 为不存在活动 `9999` 插入报名 | 写入失败 | `registration` 行数不变 |
| IC-10 | 外键约束 | 积分记录引用不存在规则 `9999` | 写入失败 | `score_record` 行数不变 |
| IC-11 | 检查约束 | 活动容量 `capacity = 0` | 写入失败 | `activity` 行数不变 |
| IC-12 | 检查约束 | 已报名人数 `registered_count > capacity` | 写入失败 | `activity` 行数不变 |
| IC-13 | 检查约束 | 活动结束时间早于开始时间 | 写入失败 | `activity` 行数不变 |
| IC-14 | 检查约束 | 报名截止时间晚于活动开始时间 | 写入失败 | `activity` 行数不变 |
| IC-15 | 检查约束 | 积分规则基础分 `base_score = -1` | 写入失败 | `score_rule` 行数不变 |
| IC-16 | 检查约束 | 积分规则普通权重 `normal_weight = 0` | 写入失败 | `score_rule` 行数不变 |
| IC-17 | 检查约束 | 积分记录最终分 `final_score = -2` | 写入失败 | `score_record` 行数不变 |
| IC-18 | 检查约束 | 积分记录身份权重 `identity_weight = 0` | 写入失败 | `score_record` 行数不变 |
| IC-19 | 枚举约束 | 账号状态写入 `LOCKED` | 写入失败 | `user_account` 行数不变 |
| IC-20 | 枚举约束 | 活动状态更新为 `ARCHIVED` | 更新失败 | 活动 `1` 原状态保持不变 |
| IC-21 | 枚举约束 | 组织成员角色写入 `CAPTAIN` | 写入失败 | `organization_member` 行数不变 |
| IC-22 | 非空约束 | 用户名写入 `null` | 写入失败 | `user_account` 行数不变 |
| IC-23 | 非空约束 | 活动名称写入 `null` | 写入失败 | `activity` 行数不变 |
| IC-24 | 非空约束 | 报名用户编号写入 `null` | 写入失败 | `registration` 行数不变 |

## 5. 执行命令

在 `backend` 目录执行：

```powershell
$env:DB_PASSWORD='Ypf200538'
& 'D:\Idea\apache-maven-3.9.9-bin\apache-maven-3.9.9\bin\mvn.cmd' test
```

只运行完整性约束测试：

```powershell
$env:DB_PASSWORD='Ypf200538'
& 'D:\Idea\apache-maven-3.9.9-bin\apache-maven-3.9.9\bin\mvn.cmd' -q -Dtest=IntegrityConstraintTest test
```

## 6. 当前验证结果

已执行后端全量测试，结果如下：

```text
Tests run: 30, Failures: 0, Errors: 0, Skipped: 0
BUILD SUCCESS
```

说明新增完整性约束测试与原有接口测试可以一起通过，数据库约束能够正确阻止非法数据写入。
